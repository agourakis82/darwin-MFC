# Darwin Design Tokens

Shared design tokens for Darwin-MFC web (Next.js) and mobile (Expo).

Exports:
- JS/TS tokens via `@darwin-mfc/design-tokens`
- CSS variables via `@darwin-mfc/design-tokens/tokens.css`

