# @darwin-mfc/protocol-data

Shared protocol datasets (flowcharts) for Darwin-MFC.

This package intentionally exports a small surface (flowchart protocols only), so
both web and mobile can share a stable data contract.

